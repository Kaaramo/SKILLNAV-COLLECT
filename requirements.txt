# SKILLNAV-DELIVERY — dépendances Python pour la collecte
# Python 3.12+

# Communs (parsing YAML, HTTP, HTML)
PyYAML>=6.0
requests>=2.31
beautifulsoup4>=4.12

# Scripts spécifiques (scripts/)
playwright>=1.45            # scrape_playwright_rekrute.py
apify-client>=1.7           # scrape_apify_linkedin.py

# Variables d'environnement attendues (à mettre dans .env) :
#   APIFY_TOKEN=apify_api_...
#   FIRECRAWL_API_KEY=fc-...
#
# Après pip install, exécuter une fois :
#   playwright install chromium
