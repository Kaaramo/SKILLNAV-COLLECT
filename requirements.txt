# SKILLNAV-DELIVERY — dépendances Python pour les scripts de collecte
# Python 3.12+ requis

# YAML I/O — utilisé par tous les scripts de restructure/enrichissement
PyYAML>=6.0.3

# HTTP — utilisé pour les appels API Apify (urllib stdlib pour rester light)
# Pas de requests requis : urllib.request stdlib suffit

# Optionnel — pour le futur (notebooks d'analyse, ingestion DB)
# pandas>=2.0
# pymongo>=4.6
# neo4j>=5.20
# elasticsearch>=8.13

# NOTE : Firecrawl est installé via npm, pas pip :
#   npm i -g firecrawl-cli
#   firecrawl --version  # doit afficher 1.x+

# NOTE : APIFY_TOKEN doit être présent dans .env (https://console.apify.com)
